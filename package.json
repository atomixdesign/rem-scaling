{
  "name": "@atomixdesign/rem-scaling-monorepo",
  "private": true,
  "version": "0.0.0",
  "author": "Jack Hillman <jack@atomix.com.au>",
  "license": "MIT",
  "scripts": {
    "dev": "lerna run dev --stream --parallel",
    "build": "lerna run build --stream",
    "clean": "rimraf packages/*/dist"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.3.5"
  }
}
